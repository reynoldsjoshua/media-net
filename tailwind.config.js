module.exports = {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {
            fontSize: {
                sm: ['14px', '20px'],
            }
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
}
